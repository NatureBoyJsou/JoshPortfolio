# Auto detect text files and perform LF normalization
* text=auto
images/PackSimDesktop.mp4 filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mp4 !text !filter !merge !diff
